---
import BaseHead from "./BaseHead.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en" class="scroll-smooth">
  <head>
    <BaseHead title={title} />
    <slot name="transition" />
  </head>
  <body class="flex min-h-screen flex-col">
    <slot />

    <!-- Analytics scripts, load last -->
    {
      import.meta.env.PUBLIC_GTM_ID && (
        <script
          is:inline
          defer
          async
          data-astro-rerun
          src="https://umami.sss-vpn.com/script.js"
          data-website-id={import.meta.env.PUBLIC_GTM_ID}
        />
      )
    }
  </body>
</html>
