---
import Base from "@/layouts/Base.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Base title={title}>
  <main
    class="container mx-auto flex h-screen w-full items-center justify-center px-4"
  >
    <slot />
  </main>
</Base>
