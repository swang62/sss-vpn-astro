---
import { Icon } from "astro-icon/components";
import ConnectButton from "@/components/ConnectButton";
import StatusBadge from "@/components/StatusBadge";
---

<section
  id="top"
  class="relative min-h-[90vh] flex items-center justify-center overflow-hidden"
>
  <div
    aria-hidden="true"
    class="absolute inset-0 z-0 pointer-events-none"
    style="mask-image: linear-gradient(to bottom, black 0%, black 92%, transparent 100%); -webkit-mask-image: linear-gradient(to bottom, black 0%, black 92%, transparent 100%);"
  >
    <div
      class="absolute left-1/2 top-[45%] -translate-x-1/2 -translate-y-1/2 opacity-60 dark:opacity-50"
    >
      <div
        class="size-125 animate-float rounded-full bg-primary/8 blur-[100px]"
      >
      </div>
    </div>
    <div class="absolute right-1/4 top-[35%] opacity-60 dark:opacity-50">
      <div
        class="size-125 animate-float rounded-full bg-secondary/8 blur-[100px]"
        style="animation-delay: -4s"
      >
      </div>
    </div>
    <div class="absolute left-1/4 bottom-[20%] opacity-60 dark:opacity-50">
      <div
        class="size-125 animate-float rounded-full bg-primary/6 blur-[80px]"
        style="animation-delay: -8s"
      >
      </div>
    </div>
  </div>

  <div
    class="relative z-10 flex flex-col items-center gap-6 px-4 text-center max-w-5xl"
  >
    <StatusBadge client:load />
    <h1
      class="font-heading text-4xl leading-[1.05] text-balance sm:text-5xl md:text-6xl lg:text-[66px]"
    >
      Welcome to Steve's{" "}
      <span class="animated-gradient-text font-extrabold">
        {" "}
        Super Secret VPN{" "}
      </span>
    </h1>

    <p
      class="text-muted-foreground max-w-xl pb-4 leading-normal text-balance sm:text-xl sm:leading-8"
    >
      Creative name I know... but if you're here, then you already know what's
      up
      <Icon
        name="mdi:face-cool"
        class="text-primary mb-1 ml-1 inline-block size-6"
      />
    </p>

    <ConnectButton client:load />
  </div>
</section>
